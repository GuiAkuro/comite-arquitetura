import { createStitches } from "@stitches/react";

export const { styled, globalCss, getCssText } = createStitches({
  theme: {
    colors: {
      primary: '#22346E',
      accent: '#A42A12',
      white: '#FFFFFF',
      surface: '#151718'
    }
  }
})